{
  "name": "use-react-intl-formatters-monorepo",
  "private": true,
  "version": "5.0.4",
  "author": "Christophe Hurpeau <christophe@hurpeau.com> (https://christophe.hurpeau.com)",
  "license": "ISC",
  "repository": "https://github.com/christophehurpeau/use-react-intl-formatters.git",
  "homepage": "https://github.com/christophehurpeau/use-react-intl-formatters",
  "type": "module",
  "packageManager": "yarn@4.9.3",
  "engines": {
    "node": ">=20.9.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn clean:build && rollup --config rollup.config.mjs && yarn run build:definitions",
    "build:definitions": "tsc -b",
    "checks": "node scripts/check-packages.js",
    "clean:build": "yarn workspaces foreach --parallel -A run clean:build",
    "generate:api": "typedoc --tsconfig tsconfig.doc.json",
    "lint": "yarn run lint:prettier && yarn run lint:eslint",
    "lint:eslint": "eslint --quiet .",
    "lint:prettier": "pob-root-prettier --check .",
    "lint:prettier:fix": "pob-root-prettier --write .",
    "postinstall": "pob-root-postinstall",
    "test": "TZ=UTC node --disable-warning=ExperimentalWarning --experimental-strip-types --test packages/*/src/**/*.test.ts",
    "test:coverage": "TZ=UTC npx c8 --all --src ./src node --disable-warning=ExperimentalWarning --experimental-strip-types --test packages/*/src/**/*.test.ts",
    "test:coverage:json": "TZ=UTC npx c8 --reporter=json --all --src ./src node --disable-warning=ExperimentalWarning --experimental-strip-types --test packages/*/src/**/*.test.ts",
    "test:coverage:lcov": "NODE_OPTIONS=--experimental-vm-modules jest --coverage --coverageReporters=lcov",
    "test:watch": "TZ=UTC node --disable-warning=ExperimentalWarning --experimental-strip-types --test packages/*/src/**/*.test.ts",
    "tsc": "tsc -b",
    "watch": "yarn clean:build && rollup --config rollup.config.mjs --watch"
  },
  "commitlint": {
    "extends": [
      "@pob/commitlint-config"
    ]
  },
  "prettier": "@pob/root/prettier-config",
  "devDependencies": {
    "@pob/commitlint-config": "9.2.0",
    "@pob/eslint-config": "61.1.0",
    "@pob/eslint-config-typescript": "61.1.0",
    "@pob/eslint-config-typescript-react": "61.1.0",
    "@pob/rollup-esbuild": "6.7.0",
    "@pob/root": "18.3.0",
    "@types/node": "22.15.2",
    "check-package-dependencies": "10.4.1",
    "eslint": "9.30.0",
    "typedoc": "0.28.11",
    "typescript": "5.9.2"
  }
}
